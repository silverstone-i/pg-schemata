# Migration scan fixtures

Files here exercise MigrationManager's directory mode: `0001_first.mjs`,
`0002_second.js`, and `0010_tenth.js` match the scan pattern; `ignored.ts`
and this README must be skipped.
